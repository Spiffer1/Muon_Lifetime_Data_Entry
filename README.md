This program will access a folder of oscilloscope screenshots. Each image should have several pulses. The program makes it easy to click
on each pulse and it then generates a list of times when each pulse occurred. After going through all the images in the folder, it 
will output a csv file with all of the pulse times.
